#include <stdio.h>
#include <stdlib.h>
/*
char nombresSalon [4][20] =
{
    "Sacoa",
    "Dinos",
    "PlayLand",
    "Coin Up"
};

char direccionSalon[4][20] =
{
    "Lavalle 505",
    "Rivadavia 2502",
    "av Cordoba 5030",
    "Gaona 3455"
};


int tipo[4] = {1,1,2,1};


int hardcodearSalones(eSalon lista[], int tam, int cant, int* pId)
{
    int contador = -1;
    if (lista != NULL && tam > 0 && cant >= 0 && cant <= tam && pId != NULL)
    {
        contador = 0;
        for (int i = 0; i < cant;  i++)
        {
            lista[i].idSalon = *pId;
            (*pId)++;
            strcpy(lista[i].nombreSalon, nombresSalon[i]);
            strcpy(lista[i].direccionSalon, direccionSalon[i]);
            lista[i].tipo = tipo[i];
            lista[i].isEmpty = 0;
            contador++;
        }
    }
    return contador;
}*/
